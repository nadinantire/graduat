<%= form_for ([@exhibition, @exhibition.comments.build]) do |f| %>
    <%= f.text_area :content, class:"single-input", placeholder:"enter comment"%>
    <%= f.submit "exhibition comment", class:"genric-btn info-border circle" %>
    <% end %>